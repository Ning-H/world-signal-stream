# OpenSignal Agent Notes

This repository follows the phase-gated build plan from the project brief.

Important operating rules:

- Stop at every human review gate.
- Stop before any external signup, paid action, API key, DNS change, or cloud resource.
- Default to local and free.
- Commit logical units of work with clear messages.
- Update `README.md` and `ARCHITECTURE.md` whenever the implementation or design changes.

